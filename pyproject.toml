[project]
name = "quack-test"
version = "0.4.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=7.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[project.entry-points.pytest11]
quack_test = "quack_test.plugin"

[dependency-groups]
dev = [
    "ty",
    "ruff",
    "pre-commit",
]
